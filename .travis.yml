sudo: required

language: python

python:
  - "3.6"

services:
  - docker

install:
  - pip install -r requirements.txt

before_script:
  - python tester.py
  - make docker-image

script:
  - docker run test
